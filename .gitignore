# macOS
.DS_Store
.AppleDouble
.LSOverride
*.swp
*.swo
*~
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
desktop.ini

# IDEs
.vscode/
.idea/
*.iml
*.sublime-workspace
*.sublime-project
*.code-workspace

# Environment files
.env
.env.local
.env.*.local

# Secrets / keys (never commit these)
*.pem
*.key
*.p12
*.pfx

# Logs
*.log
logs/

# Temporary files
*.tmp
*.temp
.cache/

# AI tooling state (checkpoints, transient logs)
.ai/

# /spawn manifests — restartable state per machine, not source
.artifacts/**/*-manifest.json

# Build outputs
dist/
coverage.out

# Test-run reports (per-machine, per-run; runners produce them on demand)
aish-windows-*-report.txt
aish-linux-ssh-report.txt
