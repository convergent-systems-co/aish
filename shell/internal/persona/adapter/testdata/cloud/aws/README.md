AWS sub-adapter fixture directory.

The aws sub-adapter uses an env-var-only strategy — no files are
read or written. This directory is reserved for future expansion
(e.g., if a v0.3-fu lands `aws_credentials_path` overrides) but is
currently empty by design.
